{% extends 'base.html.twig' %}

{% block title %}
    {{ 'game.comment.details'|trans({'%game%': game.name}) }}
{% endblock %}

{% block body %}

    <div class="container">
        <h1>{{ 'game.comment.details'|trans({'%game%': game.name}) }}</h1>
        <div class="row">
            {% include 'common/partials/comment_loop.html.twig' with
                {
                    'isHidden': 1,
                    'comments': game.comments,
                    'classCss': 'col-20'
                }
            %}
        </div>
    </div>

{% endblock %}
